<!--index.wxml-->
<view class="container" id="container" catchtouchmove="noop">
  <block wx:for="{{demos}}" wx:for-item="item" wx:key="item.url">
    <navigator url="{{item.url}}" hover-class="navigator-hover">{{item.label}}</navigator>
  </block>
</view>
